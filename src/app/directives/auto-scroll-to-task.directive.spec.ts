import { AutoScrollToTaskDirective } from './auto-scroll-to-task.directive';

describe('AutoScrollToTaskDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoScrollToTaskDirective();
    expect(directive).toBeTruthy();
  });
});
