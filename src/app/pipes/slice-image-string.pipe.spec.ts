import { SliceImageStringPipe } from './slice-image-string.pipe';

describe('SliceImageStringPipe', () => {
  it('create an instance', () => {
    const pipe = new SliceImageStringPipe();
    expect(pipe).toBeTruthy();
  });
});
