import { TextAnalysisPipe } from './text-analysis.pipe';

describe('TextAnalysisPipe', () => {
  it('create an instance', () => {
    const pipe = new TextAnalysisPipe();
    expect(pipe).toBeTruthy();
  });
});
