import { SeventaskDatePipePipe } from './seventask-date-pipe.pipe';

describe('SeventaskDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new SeventaskDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
