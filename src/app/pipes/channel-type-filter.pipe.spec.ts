import { ChannelTypeFilterPipe } from './channel-type-filter.pipe';

describe('ChannelTypeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ChannelTypeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
