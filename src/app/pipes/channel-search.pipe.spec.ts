import { ChannelSearchPipe } from './channel-search.pipe';

describe('ChannelSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ChannelSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
