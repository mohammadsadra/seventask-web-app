import { ArbicNumberDatePipe } from './arbic-number-date.pipe';

describe('ArbicNumberDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ArbicNumberDatePipe();
    expect(pipe).toBeTruthy();
  });
});
