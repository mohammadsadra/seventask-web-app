import { LinkFinderPipe } from './link-finder.pipe';

describe('LinkFinderPipe', () => {
  it('create an instance', () => {
    // @ts-ignore
    const pipe = new LinkFinderPipe();
    expect(pipe).toBeTruthy();
  });
});
