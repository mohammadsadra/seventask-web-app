import { CastToFilterPriorityModelPipe } from './cast-to-filter-priority-model.pipe';

describe('CastToFilterPriorityModelPipe', () => {
  it('create an instance', () => {
    const pipe = new CastToFilterPriorityModelPipe();
    expect(pipe).toBeTruthy();
  });
});
