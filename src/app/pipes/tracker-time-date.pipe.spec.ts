import { TrackerTimeDatePipe } from './tracker-time-date.pipe';

describe('TrackerTimeDatePipe', () => {
  it('create an instance', () => {
    const pipe = new TrackerTimeDatePipe();
    expect(pipe).toBeTruthy();
  });
});
