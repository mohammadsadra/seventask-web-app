import { ManageDatePipe } from './manage-date.pipe';

describe('ManageDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ManageDatePipe();
    expect(pipe).toBeTruthy();
  });
});
